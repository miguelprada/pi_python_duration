sudo: false

language:
  - python

python:
  - 3.6

install:
  - pip install -r requirements.txt

script:
  - pycodestyle src/pi_sample_duration
  - pytest --verbose --capture=no src/sample_duration
  - pip install .

notifications:
  email: false
